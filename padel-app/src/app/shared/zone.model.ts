export class Zone {
    public name: string;
    public amount: number;

    constructor(name: string, amount: number) {
        this.name = name;
        this.amount = amount;
    }

    //constructor (public name: string, public amount: number) {}  se puede hacer asi tmb

}