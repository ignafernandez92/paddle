const express = require('express');
const router = express.Router();

// This route should return the user's ID based on the authentication middleware setup
router.get('/', (req, res) => {
  const { user_id } = req.user;
  res.json({ user_id });
});

module.exports = router;
